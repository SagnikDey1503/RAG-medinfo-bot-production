# --- API / server ---
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# --- LLM / embeddings ---
openai>=1.40

# --- Retrieval ---
faiss-cpu>=1.8
numpy>=1.26,<2.0
rank-bm25>=0.2.2

# --- Ingestion ---
pypdf>=4.2
langchain-text-splitters>=0.2

# --- Reranker (cross-encoder). Heavy (pulls torch); optional at runtime:
#     if unavailable the pipeline falls back to an LLM reranker. ---
sentence-transformers>=3.0

# --- Optional CRAG web-search fallback (needs TAVILY_API_KEY) ---
tavily-python>=0.3
